/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['system-ui', 'sans-serif'], // pour le texte général
        title: ['system-ui', 'sans-serif'], // police spécifique pour les titres
      },
    },
  },
  plugins: [],
}
